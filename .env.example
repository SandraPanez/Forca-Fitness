PORT=3000

# Database Configuration (PostgreSQL)
# Para desarrollo local (Temporal)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=forca_fitness

# Para producción (AWS / Render - Descomentar y llenar cuando se despliegue)
# DATABASE_URL=postgresql://usuario:password@host:puerto/nombre_bd
